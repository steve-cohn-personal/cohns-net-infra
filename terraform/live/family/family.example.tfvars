# Copy to family.auto.tfvars (gitignored, auto-loaded) and fill in the real ARN.
# The library lives in the account that serves site content (prod).

account_role_arn = "arn:aws:iam::<prod-account-id>:role/OrganizationAccountAccessRole"
